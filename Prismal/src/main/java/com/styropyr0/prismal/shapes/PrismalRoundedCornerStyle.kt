package com.styropyr0.prismal.shapes

/** Corner radius style used when building rounded Prismal shapes. */
enum class PrismalRoundedCornerStyle {

    /** Standard circular arc corners. */
    Circular,

    /** Continuous curvature (squircle-like) corners. */
    Continuous
}
